import { NutrientAttributes } from '@/interfaces/recipe.interface';
import { DataTypes, Model, Sequelize } from 'sequelize';



export default async (db: Sequelize): Promise<any> => {
    // return 
};
