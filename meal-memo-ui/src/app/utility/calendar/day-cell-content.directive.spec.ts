import { DayCellContentDirective } from './day-cell-content.directive';

describe('DayCellContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DayCellContentDirective();
    expect(directive).toBeTruthy();
  });
});
